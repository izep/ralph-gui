---
name: Headless exit POC
epic: poc-headless-exit
status: pending
---

# Headless exit

When the plan agent reports the epic complete, Ralph must stop the loop. With `--exit-when-complete`, the server process must exit instead of idling.

**Success:** one plan call, empty backlog, process gone.
