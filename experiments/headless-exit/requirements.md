# Headless exit POC

Ralph must end the loop when there is no remaining work and, in `--start --exit-when-complete` mode, the Node process must exit.

There is no application to build. A stub agent is used instead of Copilot.
