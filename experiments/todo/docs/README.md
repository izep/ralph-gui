# Todo

Documentation screenshots and other static reference files for this experiment. They are **not** bundled into the Vite app.

## `board.png` — Ralph Kanban with this folder as `--repo` (running loop)

![Ralph Kanban with this folder as `--repo` (running loop)](./board.png)

## `todo-complete.png` — Todo UI after the loop has implemented the product under `src/`

![Todo UI after the loop has implemented the product under `src/`](./todo-complete.png)
